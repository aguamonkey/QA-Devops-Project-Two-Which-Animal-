all:
  children:
    managers:
      hosts:
        docker-manager:
    workers:
      hosts:
        docker-swarm:
    
    nginx:
      hosts:
        nginx:
  vars:
    ansible_python_interpreter: "/usr/bin/python3"
  
