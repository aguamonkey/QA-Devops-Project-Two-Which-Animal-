all:
  children:
    managers:
      hosts:
        docker-manager:
    workers:
      hosts:
        docker-swarm:
        docker-swarm-two:
    
    nginx:
      hosts:
        nginx:
  vars:
    ansible_python_interpreter: "/usr/bin/python3"
  
